kind: Task
apiVersion: mcpchecker/v1alpha2
metadata:
  name: pull-requests
  difficulty: medium
  labels:
    suite: prs
    category: basic
  parallel: true
spec:
  prompt:
    inline: |
      How many pull requests are currently tracked for the openshift org in the
      CI system? List a few with their titles and current status.
